import { xs, sm } from "@Utils/mediaQueries";
import styled from "styled-components";
import tw from "twin.macro";

export const Wrapper = styled.div`
  ${apply`flex py-8`};
`;
export const TagItem = styled.div`
  ${apply`mr-6 text-lg font-semibold`};
`;
